- name: Install redis on Rocky Linux
  ansible.builtin.dnf:
    name: redis
    state: present

- name: Ensure redis is running and enabled
  ansible.builtin.service:
    name: redis
    state: started
    enabled: true
